# Milestone Update: Next.js 16 Upgrade

## Execution Timestamp
Executed on: 2026-08-05 at 14:23:53 GMT

## Milestone Summary
Pushed commit `1e6063c` to `callfelixstudios/property-hub-gh` (branch `main`): upgraded Next.js from `15.4.0` to `^16.3.0`. The upgrade required adding `@babel/core` as a devDependency (required by `eslint-plugin-react-hooks` after the upgrade), reverting `jsx` in `tsconfig.json` from `preserve` to `react-jsx`, and removing the `edge` runtime from the OG image route (deprecated in Next 16). The previous SEO/type-safety milestone log was also added to the Obsidian vault. Verified: `npx tsc --noEmit` passes with 0 errors, `npm run build` succeeds (all 25 pages), `npm run lint` passes with 0 errors (31 non-blocking warnings).

## Files Modified
- `package.json`
- `package-lock.json`
- `tsconfig.json`
- `src/app/opengraph-image.tsx`
- `obsidian-vault/2026-08-05-1409-seo-and-type-safety-push.md`

## Data/UI Architecture State
- **Next.js**: now at `v16.3.0` (was `15.4.0`).
- **tsconfig**: `jsx: react-jsx` (reverted from `preserve`).
- **OG image**: now runs on Node runtime (edge runtime removed); statically prerendered — shows ○ (static) in build output.
- **ESLint**: `eslint-plugin-react-hooks` now depends on `@babel/core`, which was added as a devDependency.
- **Lint warnings**: 31 non-blocking warnings remain (unused vars, `<img>` tags, `useEffect` deps).
- **Build warning (non-blocking)**: dynamic font download failed (Status 400) on the opengraph-image build.

## Next Immediate Steps
- Fix the 31 lint warnings (unused vars, `<img>` tags, `useEffect` deps).
- Investigate the dynamic font download (Status 400) warning on the `opengraph-image` build.
- Verify app functionality against Next 16 breaking changes (async `searchParams` already handled).
- Consider adding a `typecheck` npm script.
